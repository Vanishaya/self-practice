1. ใช้ map() เพื่อวนผ่านทุกค่าใน arr แล้วเปลี่ยนแต่ละตัวเป็น ยกกำลังสอง ใช้ filter() เพื่อ คัดออก เหลือเฉพาะตัวที่เป็น คี่
ใช้ reduce() รวมค่าทั้งหมดใน array ให้เหลือค่าเดียว (ผลรวม)

2.ใช้สร้าง array ที่มีขนาดตาม length ที่กำหนด .fill(value) ใช้เติม (fill) ค่าเดิมซ้ำ ๆ ลงไปในทุกตำแหน่งของ array
ค่าในวงเล็บ value คือค่าที่เราต้องการให้เต็มทุกช่อง

3.arr.filter(...)filter() จะวนดูสมาชิกทุกตัวใน arr คืนค่าใหม่เป็น array ที่เก็บเฉพาะค่าที่ "ผ่านเงื่อนไข"
Boolean เป็นฟังก์ชันใน JS ที่จะแปลงค่าหนึ่ง ๆ ให้เป็น true หรือ false

4.ตรวจสอบ inputถ้าเป็น null, undefined, หรือ "" → return "" เลย แยกคำด้วย .split(" ")
"This is is a test" → ["This", "is", "is", "a", "test"]ใช้ Set เก็บคำที่เจอแล้ว
เวลาเปรียบเทียบใช้ .toLowerCase() เพื่อให้ case insensitive เช่น "Hello" และ "hello" ถือว่าเหมือนกัน เก็บเฉพาะคำแรกที่เจอ
result.push(word) จะเก็บคำในรูปเดิม (case เดิม) รวมคำกลับเป็นประโยค ใช้ .join(" ")

5.ตรวจสอบ input ถ้าเป็น null หรือ undefined → return undefined ทันที แปลงเป็น lowercase
แยกเป็นคำด้วย .split(" ")วนลูปเก็บความถี่ คืนค่า object