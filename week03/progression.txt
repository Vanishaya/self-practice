1. หาผลรวมของตัวเลขในอาเรย์ function sumArray(numbers) ที่รับพารามิเตอร์เป็น array ของตัวเลข แล้วใช้ for loop 
หาผลรวมของทุกตัวเลข เช็คก่อนว่าค่านั้นเป็นตัวเลขจริง ๆ แล้วค่อยบวก  typeof + Number.isFinite

2. แปลงตัวอักษรให้เป็นพิมพ์ใหญ่ function toUpperCaseArray(words) ที่รับ array ของ string แล้วคืนค่า array 
ใหม่ที่เป็นตัวอักษร พิมพ์ใหญ่ทั้งหมด แต่ถ้ามี i ในประโยคไม่ว่าจะพิมพ์เล็กหรือใหญ่จะเปลี่ยนเป็นพิมพ์เล็กทั้งหมดทุกตำแหน่งที่เจอ

3. หานักเรียนที่มีอายุมากที่สุด ให้มี array ของ object นักเรียน function findOldestStudent(students) 
ที่คืนค่า object ของนักเรียนที่มี อายุมากที่สุด

4. เช็คว่ามีเลขอยู่ในอาเรย์ function containsNumber(numbers, target) ที่รับ array ของตัวเลข เช็คว่าเป็นตัวเลขมั้ย 
ถ้าใช่ แล้วคืนค่า true ถ้ามีเลขนั้นอยู่ในอาเรย์ และ ถ้าไม่ใช่ตัวเลขหรือไม่มีให้คืนค่า false

5. หาค่าเฉลี่ยของตัวเลข function average(numbers) ับ array ของตัวเลข คืนค่าเฉลี่ยของตัวเลขทั้งหมด ข้ามค่าที่ไม่ใช่ตัวเลข